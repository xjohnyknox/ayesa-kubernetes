Crea un Deployment  llamado `api` para que:

* use la imagen `nginx:latest`
* tenga 2 réplicas
* use `nodeSelector: env=prod
* tolere `maintenance=true:NoSchedule`

Aplica tu parche en **respuesta.txt**.
